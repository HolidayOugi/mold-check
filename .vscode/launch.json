{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run mold-check",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/renderMSVC-release/mold-check.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}/build/renderMSVC-release",
            "environment": [],
            "console": "integratedTerminal",
            "preLaunchTask": "CMake: build mold-check"
        }
    ]
}
